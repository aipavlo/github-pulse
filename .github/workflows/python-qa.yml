name: Python QA

"on":
  pull_request:
    paths:
      - ".github/workflows/python-qa.yml"
      - "ingestion/**"
      - "orchestration/**"
      - "publish/**"
      - "tests/**"
      - "pyproject.toml"
      - "requirements.txt"
  push:
    branches:
      - main
    paths:
      - ".github/workflows/python-qa.yml"
      - "ingestion/**"
      - "orchestration/**"
      - "publish/**"
      - "tests/**"
      - "pyproject.toml"
      - "requirements.txt"

permissions:
  contents: read

jobs:
  qa-python:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Lint
        run: ruff check ingestion orchestration tests

      - name: Test
        run: pytest
